# Launchpad Clone

一个仿 LaunchOS / 原生启动台的 macOS 应用启动器,原生 Swift + SwiftUI 编写。

> 项目理念(与 LaunchOS 一致):不堆功能,专注打磨那些"消失时会立刻被察觉"的细节体验。

## 当前状态:v0.5 横向分页

- [x] 扫描系统与用户应用目录(`/Applications`、`/System/Applications`、`~/Applications` 等)
- [x] 全屏网格展示应用图标(自适应列数)
- [x] 实时搜索过滤(输入即搜)
- [x] 点击图标启动应用
- [x] 毛玻璃(ultraThinMaterial)背景
- [x] **全屏无边框覆盖层**(盖在普通窗口与 Dock 之上,不占 Dock 图标)
- [x] **`Esc` 关闭**;搜索非空时 `Esc` 先清空搜索
- [x] **点击空白区域关闭**
- [x] **显示后自动聚焦搜索框**,无需点击直接输入
- [x] **菜单栏图标**:左键唤起/关闭,右键菜单(显示/隐藏、退出)
- [x] **全局快捷键 `⌥空格`** 随时唤起/关闭(Carbon API,无需辅助功能权限)
- [x] **关闭 = 隐藏待唤起**,常驻后台,焦点自动还给之前的应用
- [x] **重复执行唤起动作即关闭**(和原生 Launchpad 一致)
- [x] **布局持久化**:应用/组/来源三实体模型(页面与文件夹统一抽象),存 `~/Library/Application Support/LaunchpadClone/layout.json`
- [x] **自动对账**:每次唤起时重新扫描 —— 新装的追加到末尾,卸载的自动移除,已有的保持顺序
- [x] 模型层支持**别名**(搜索按显示名/默认名双路匹配)与**隐藏**(界面操作后续做)
- [x] **横向分页浏览**:固定 7×5 网格,超容自动溢出到下一页并落盘
- [x] **跟手拖拽翻页**:拖拽位移实时更新,按预测终点(含甩动速度)决定翻页
- [x] **滚轮/触控板翻页**:带冷却防抖,一次滚动只翻一页(平滑滚轮/惯性兼容)
- [x] **`⌘←` / `⌘→` 键盘翻页**(原生同款快捷键)
- [x] **页码圆点指示器**,可点击跳页
- [x] **记住上次页码**:隐藏再唤起停留在原页(原生默认行为)
- [x] **搜索回车直接打开第一个结果**

## 构建与运行

```bash
./build.sh
./launchpad
```

> 说明:本机 SwiftPM 清单库损坏,`build.sh` 直接用 `swiftc` 编译。
> 装完整版 Xcode 后可改用 `swift build`,或用 Xcode 打开 `Package.swift`。

## 路线图(按 LaunchOS 功能清单分阶段)

### 阶段 1 — 核心体验
- [x] 全屏无边框窗口(真正的 Launchpad 式覆盖层)
- [x] `Esc` 关闭 / 点击空白关闭
- [ ] 键盘导航(方向键高亮 + 回车打开)
- [ ] 搜索:拼音匹配、首字母匹配、缩写匹配

### 阶段 2 — 唤起方式
- [x] 菜单栏图标唤起
- [x] 全局快捷键唤起(暂固定 `⌥空格`,自定义组合键待做)
- [ ] 接管 F4 键(需要辅助功能权限)
- [ ] 触发角唤起

### 阶段 3 — 分页与整理
- [x] 横向分页(纵向滚动模式待做)
- [x] 滚轮/拖拽/键盘三种翻页方式(跟手动画)
- [ ] 拖拽排序、拖拽跨页
- [ ] 文件夹(创建/合并/解散/重命名)
- [ ] 自定义排序模式持久化

### 阶段 4 — 深度打磨
- [ ] 触控板捏合手势唤起(跟手动画)
- [ ] 导入原生 Launchpad 布局
- [ ] 多显示器策略
- [ ] 布局备份与恢复
- [ ] 右键菜单(重命名/隐藏/在 Finder 显示/卸载)

## 项目结构

```
Sources/LaunchpadClone/
├── main.swift         # 应用入口:覆盖层窗口 + 菜单栏图标 + 全局快捷键 + Esc
├── AppScanner.swift   # 扫描应用来源目录
├── LayoutStore.swift  # 布局持久化:应用/组/来源模型 + 对账 + 落盘
└── ContentView.swift  # 主界面:搜索框 + 图标网格 + 空白点击关闭
```
